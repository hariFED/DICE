# Rust
target/

# Anchor / Solana
.anchor/
test-ledger/
*.so

# Node.js
node_modules/
package-lock.json

# Firmware
firmware/build/
firmware/managed_components/
firmware/dependencies/
firmware/.cache/
sdkconfig
sdkconfig.old
firmware/tools/prov_output/

# Next.js
.next/
# TypeScript incremental build cache (regenerates on every tsc run)
tsconfig.tsbuildinfo
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
dist/
.venv/
venv/

# Build artifacts
build/reports/*.xml
build/reports/*.html
build/state.json
# Device provisioning output — contains secp256k1 private keys + mTLS certs.
# NEVER commit.
build/v7_nvs*

# Runtime logs
coordinator.log
coordinator-*.log

# Secrets — NEVER commit these
coordinator-keypair.json
*-keypair.json
*.key
*.pem
*.p12
pki/certs/root_ca.key
pki/certs/intermediate_ca.key
infra/secrets/*.yaml
!infra/secrets/.sops.yaml
.env
*.env

# OS / IDE / Tools
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/
.claude/

.vercel
