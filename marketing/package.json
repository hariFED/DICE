{
  "name": "dice-marketing",
  "version": "0.1.0",
  "private": true,
  "description": "DICE marketing kit — HTML slides, cards, brandbook. Builds to PDF via headless Chromium.",
  "scripts": {
    "pdf": "node build-pdfs.mjs",
    "pdf:serve": "npx -y serve src -l 4000"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
