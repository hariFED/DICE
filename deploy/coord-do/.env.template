# DICE Coordinator · runtime env
# Copy this to `.env` (same folder), fill in, DO NOT commit.
#
#   cp deploy/coord-do/.env.template deploy/coord-do/.env

# ── Postgres (Neon) ────────────────────────────────────────────────────────
# Primary: US-East (matches DO NYC3 region for low latency)
DATABASE_URL=postgresql://neondb_owner:npg_mr5BDTjnRFz9@ep-rough-unit-amap2seh-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Fallback: AP-Southeast-1. The coord itself doesn't hot-failover between
# URLs today — this is for documentation + manual swap if Neon primary is
# down. (To actually use it: edit .env on the droplet, docker compose restart.)
DATABASE_URL_FALLBACK=postgresql://neondb_owner:npg_BAiOTSkw78XU@ep-little-term-aocc2hb8-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# ── Solana RPC (Helius devnet) ─────────────────────────────────────────────
SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=d9fee3e4-8efe-43f6-b7bb-ba085eb44b46

# Fallback Helius key — swap into SOLANA_RPC_URL if primary is rate-limited
SOLANA_RPC_URL_FALLBACK=https://devnet.helius-rpc.com/?api-key=a3b1505b-5d1e-4221-a347-ae129f7d6115

# ── Payout wallets (from v7.7 deploy) ──────────────────────────────────────
DICE_TREASURY=C2JugYQztp1XDGG1ZCagbqRivqGsmE1vG1uMHaMHPDaQ
DICE_RESERVE=3df8FZoosdv3mrYwWS82TEqQps97qAdmnnijUNhz6tp9

# ── API auth + rate limiting ───────────────────────────────────────────────
# Generate with:  openssl rand -hex 32
DICE_API_KEY=REPLACE_WITH_OPENSSL_RAND_HEX_32
DICE_RATE_LIMIT_RPS=50
